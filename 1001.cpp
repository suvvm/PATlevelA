#include <bits/stdc++.h>
#include <ctype.h>
using namespace std;

int main()
{
    int a, b, c;
    cin >> a >> b;
    c = a + b;
    char d[20];
    sprintf(d, "%d", c);

    cout << d;
    return 0;
}
